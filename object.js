const users = {
    name : 'rahim',
    age : 22,
    adress : 'kamlapur'
};

// const {name : userName, adress : userAdress}= users;
// console.log(users);

const {name : userName, age : userAge , adress: userAdress} = users;
console.log(userName,userAge );