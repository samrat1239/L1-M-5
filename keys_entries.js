const applicants = {
    name : "abul kashem",
    skill : "software engineer",
    adress :"dhaka"
};

const keys = Object.keys(applicants);
console.log(keys);

const entries = Object.entries(applicants);
console.log(entries);
