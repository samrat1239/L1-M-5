const applicant =
{
    name : 'Md Rahul khan',
    age : 22,
    isMarried : false,
    isTeacher : false,
    isStudent : true,
    adress : "Kamlapur School para"
};

// console.log(applicant);

const applicantJson = JSON.stringify(applicant);
console.log(applicantJson);